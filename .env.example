# Environment Configuration for Zeroday Academy

# Application Environment
NODE_ENV=production

# Database Configuration
# Replace with your PostgreSQL connection string
DATABASE_URL=postgresql://zeroday_user:your_secure_password@localhost:5432/zeroday_academy

# Server Configuration
PORT=5000

# Session Security
# Generate a secure random string for session encryption
SESSION_SECRET=your_very_long_random_session_secret_change_this_in_production

# Optional: Enable debug logging (development only)
# DEBUG=express:*